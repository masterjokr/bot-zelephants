services:
  - type: web
    name: bot-zelephants-discord
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node bot.js
    envVars:
      - key: DISCORD_BOT_TOKEN
        sync: false
